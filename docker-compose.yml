version: '2'

services:
    web:
        image: nginx:latest
        ports:
            - "8080:80"
        volumes:
            - ./src:/app
            - ./site.conf:/etc/nginx/conf.d/default.conf
        networks:
            - code-network
        links:
          - php

    php:
        image: php:fpm
        volumes:
            - ./src:/app
        networks:
            - code-network
        ports:
            - "8082:8001"

    composer:
        restart: 'no'
        image: composer/composer:php7
        command: install
        volumes:
          - ./src:/app


networks:
    code-network:
        driver: bridge